<h1>記事一覧</h1>

<!-- 記事作成フォームを上部に埋め込む -->
<h2>新しい記事を作成</h2>
<%= render 'form', article: @article %>

<hr>

<h2>記事一覧</h2>
<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article) %>
    </li>
  <% end %>
</ul>
